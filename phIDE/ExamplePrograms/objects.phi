# Phi supports objects/hashmaps/dictionaries.

obj o = {
    a:1,
    b:[1, 2, 3],
    c:{
        d:"hello"
    }
    e:fn add(a, b) {
		<- a + b
	}
}

output(o.a)
output(o.b)
output(o.c)
output(o.e(4, 3))


