export obj class = {
	init:fn ~(values){

		if (class.attrs.length() != values.length()){
			throw syntaxError
		}

        obj cls = {}
		str a = ""

		for (int i, i < values.length(), i += 1 ){
			a = class.attrs[i]
			cls[a] = values[i]
		}

		<- cls
    }
}