

class Person {
    constr: fn ~(name, age){
        self.name = name
        self.age = age
    },
    getName: fn ~(){<- self.name}
}

# ----------
# obj person = {
#     name:"john",
#     age:26,
#     sayHi:fn sayHi(){}
# }

