import classes

classes.attrs = ['name', 'age', 'sayHi']

lambda sayHi = fn ~(){<- "hi"}

array values = ["John", 32, sayHi]
obj person = classes.class(values)


output(person)


output(person.name)
output(person.age)
output(person.sayHi())




