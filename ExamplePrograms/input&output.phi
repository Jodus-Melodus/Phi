# Phi has it's own built-in in and output functions

var x = in("> ")    # Assigns the variable 'x' to the string value of the input from the terminal

out(x)  # Prints/Displays the value in the terminal
