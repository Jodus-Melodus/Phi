# Phi supports objects/hashmaps/dictionaries.

var obj = {
    a:1,
    b:[1, 2, 3],
    c:{
        d:"hello"
    }
    e:fn add(a, b) {
		<- a + b
	}
}

out(obj.a)
out(obj.b)
out(obj.c)
out(obj.e(4, 3))


